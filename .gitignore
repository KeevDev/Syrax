build/
.cache/
compile_commands.json
uploads/

# Lista blanca de directorios en la raiz. Cualquier otro se ignora, incluidos
# los proyectos de prueba que uno genera aqui sin querer: ya paso dos veces
# (api-sqlite/, dkv/) y los patrones por nombre no los atraparon.
/*/
!/.github/
!/cli/
!/docs/
!/example/
!/include/
!/tests/
